<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> | My Page</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #f8fafc 0%, #e0e7ff 100%);
            min-height: 100vh;
        }
        .card {
            box-shadow: 0 4px 24px rgba(0,0,0,0.08);
        }
        .hero {
            background: #6366f1;
            color: #fff;
            padding: 2rem 1rem;
            border-radius: 1rem;
            margin-bottom: 2rem;
        }
        .footer {
            margin-top: 3rem;
            padding: 1rem 0;
            background: #f1f5f9;
            text-align: center;
            color: #64748b;
            border-radius: 0.5rem;
        }
    </style>
</head>
<body>
    <div class="container py-5">
        <div class="hero text-center mb-4">
            <h1 class="display-4 fw-bold mb-2">Welcome to My Page</h1>
            <p class="lead mb-0">Explore, learn, and enjoy!</p>
        </div>
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card p-4 mb-4">
                    <h3 class="card-title text-primary mb-3"><%= title %></h3>
                    <p class="card-text fs-5"><%= message %></p>
                    <hr>
                    <ul class="list-group list-group-flush mb-3">
                        <li class="list-group-item">✨ Modern responsive design</li>
                        <li class="list-group-item">🚀 Powered by Bootstrap 5</li>
                        <li class="list-group-item">📚 Easy to customize</li>
                    </ul>
                    <a href="#" class="btn btn-primary">Learn More</a>
                </div>
            </div>
        </div>
        <div class="footer">
            &copy; <%= new Date().getFullYear() %> My Awesome Page. All rights reserved.
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>